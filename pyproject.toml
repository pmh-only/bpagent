[project]
name = "bpagent"
version = "0.1.0"
description = "AWS Well-Architected Audit Agent powered by AWS MCP & StrandsAgents"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.35.0",
    "pydantic>=2.12.5",
    "strands-agents[openai]>=1.27.0",
    "rich>=13.0.0",
]
